[project]
name = "nmap"
version = "2.1.0"
description = "rustscan_massive - Network scanning tool with web bruteforce"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.40.0",
]
